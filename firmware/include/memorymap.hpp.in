#pragma once

// clang-format off

/**
 * @brief Application image header start.
*/
#define APP_HEADER_START (@APP_HEADER_START@UL)

/**
 * @brief Application image header size.
*/
#define APP_HEADER_SIZE (@APP_HEADER_SIZE@UL)

/**
 * @brief Application image header ID.
*/
#define APP_HEADER_ID (@APP_HEADER_ID@UL)

/**
 * @brief Application code area start.
*/
#define APP_CODE_START (@APP_CODE_START@UL)

/**
 * @brief Application code area size.
*/
#define APP_CODE_SIZE (@APP_CODE_SIZE@UL)

/**
 * @brief Flash area start.
*/
#define DEVICE_FLASH_START (@FLASH_BASE@UL)

/**
 * @brief Flash area size.
*/
#define DEVICE_FLASH_SIZE (@FLASH_SIZE@UL)

// clang-format on
