#include "common.hpp"

/**
 * @brief Application header used by the bootloader to verify the image.
 */
__attribute__((section(".header")))
__attribute__((used))
AppHeader header = {
	// ID
	0,
	// Size
	0,
	// CRC
	0,
	// Metadata
	{
		"@TIMESTAMP@",
		"@GIT_HASH@",
		"@TOOLCHAIN_VER@"
	}
};
